apiVersion: apps/v1
kind: Deployment
metadata:
  name: kb-orchestrator
  namespace: orchestrator
spec:
  template:
    metadata:
      labels:
        kb-signed: "true"          # 触发门控
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: api
          image: ghcr.io/bbk66669/kingbrain/orchestrator@sha256:REPLACE_WITH_DIGEST
