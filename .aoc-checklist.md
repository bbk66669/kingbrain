# AOC Checklist (v1)

> 本清单用于 **Reviewer 真校验**。任意一条不满足即**拒绝**，并给出可复现实验与修复建议。  
> 产物路径对齐当前实现：LangGraph（Composer） + Orchestrator SafeIO + allowlist。

---

## ✅ AOC-1: PoR / Spec 对齐（POR/Spec alignment）
**要点**  
- 变更必须与 `docs/kingbrain/POR.md` 与 `/.collab/spec.lock.yaml` 一致。
- PLAN 中要明确“范围、非目标、验收标准”。

**证据**  
- `docs/kingbrain/PLAN/PLAN.md`（或 `docs/kingbrain/PLAN/result-*.md`）
- `/.collab/spec.lock.yaml`、`docs/kingbrain/POR.md`

**自动机检（示例）**  
- `kb tools check-por --por docs/kingbrain/POR.md --plan docs/kingbrain/PLAN/PLAN.md --spec ./.collab/spec.lock.yaml`

**失败提示**  
> AOC-1 失败：PLAN 未引用 POR/Spec 或范围/验收未明确（请补充 PLAN 的 “What/Why/How/Acceptance” 小节）。

---

## ✅ AOC-2: 源码来源与许可证（Sources & Licenses）
**要点**  
- manifest 列出所有外借模板/仓库 **repo+tag/commit+license**。  
- BORROW 阶段必须附 `Sources.md` 或在 `README.md` 标注来源表格。

**证据**  
- `docs/kingbrain/BORROW/README.md` 或 `docs/kingbrain/BORROW/Sources.md`  
- `docs/kingbrain/PLAN/manifest.json`（或 `manifest-*.json`）  
- `data/sbom/**`（SBOM）

**自动机检（示例）**  
- `kb tools license-scan --policy=configs/sbom-policy.yaml`  
- `kb tools sbom-verify --format spdx-json@3.0 --path=data/sbom/`

**失败提示**  
> AOC-2 失败：缺少来源/许可证清单或与 SBOM 不一致（请补充 manifest 与 BORROW 文档，并修正 SBOM）。

---

## ✅ AOC-3: 因果映射（Causal mapping）
**要点**  
- 变更 → 受影响组件 → 触发事件（CloudEvents/NATS）要有**因果链**。  
- PLAN 中提供“Causal Mapping”小节，至少 3 层映射。

**证据**  
- `docs/kingbrain/PLAN/PLAN.md` 的 “Causal Mapping” 小节  
- `.collab/audit/events-*.jsonl`（或 NATS 消息快照）

**自动机检（示例）**  
- `kb tools causal-validate --plan docs/kingbrain/PLAN/PLAN.md --events .collab/audit/`

**失败提示**  
> AOC-3 失败：未提供完整因果映射或事件证据不足（请补齐 Mapping 与对应事件 ID）。

---

## ✅ AOC-4: 差异最小（Unified diff only）
**要点**  
- **只允许统一 diff**（不粘贴整文件）。  
- 仅触达 `/.collab/paths.allowlist.yaml` 中允许的路径。

**证据**  
- `docs/kingbrain/DIFF/*.patch`

**自动机检（示例）**  
- `kb tools diff-validate --patch-glob "docs/kingbrain/DIFF/*.patch" --allowlist ./.collab/paths.allowlist.yaml`

**失败提示**  
> AOC-4 失败：存在整文件粘贴或越权修改非 allowlist 路径（请改为最小 diff，并修正目标路径）。

---

## ✅ AOC-5: 证据包（SBOM/签名/策略）
**要点**  
- 产物需附 **SBOM**、**Cosign/签名摘要**、**Kyverno dry-run 结果**，并在 CR 中引用。

**证据**  
- `data/sbom/**`、`reports/**`（kyverno 验证输出）、Cosign 摘要（`sha256:...`）

**自动机检（示例）**  
- `kb tools sbom-verify --path data/sbom/`  
- `kb tools kyverno-verify ./k8s/manifests`  
- `kb tools attestation-verify --cosign-digest <sha256>`

**失败提示**  
> AOC-5 失败：证据包缺失或摘要无效（请补充 SBOM/签名/策略验证产物并在 CR 引用）。

---

## ✅ AOC-6: 回滚路径（Rollback path）
**要点**  
- 明确 **镜像 digest / Git tag** 的回滚方案（指向稳定工件）。  
- Kustomize/Helm patch 使用 **`@sha256:` 固定**镜像。

**证据**  
- `k8s/**/patch-image.yaml`、`k8s/**/patch-*-image.yaml` 中的 `image: repo@sha256:...`  
- PLAN 的 “Rollback” 小节

**自动机检（示例）**  
- `kb tools rollback-validate --kustomize ./k8s/orchestrator/overlays/real/ --require-digest`

**失败提示**  
> AOC-6 失败：未固定镜像 digest 或缺少回滚步骤（请 pin 到 @sha256 并在 PLAN/CR 写明回滚命令）。

---

## ✅ AOC-7: 后置验证（Post-verification）
**要点**  
- 合并后需验证：Prometheus 指标、OpenLineage 图、OPA/Kyverno 决策日志。  
- 给出可复现实验命令与目标阈值（SLO）。

**证据**  
- `reports/kb-validate.xml`（JUnit 汇总）  
- 观测/日志片段或链接（可脱敏）

**自动机检（示例）**  
- `kb tools validate --config ./.collab/kb-validate/config.yaml`

**失败提示**  
> AOC-7 失败：后置验证不达标或缺少报告（请补充验证步骤、指标阈值与报告附件）。

---

## 全局硬性规则（Reviewer 始终检查）
- **只写 allowlist**：参照 `/.collab/paths.allowlist.yaml`；越界即拒。  
- **禁止明文密钥**：提交中不得出现密钥/令牌/私钥。  
- **CR 模板**：`reports/CR/cr-draft.yaml` 或 PR 描述里必须引用 **diff 链接**与**证据摘要**。

---

## Machine-Readable (YAML)

```yaml
version: 1
require_all: true

rules:
  - id: AOC-1
    name: POR/Spec alignment
    must_pass: true
    evidence:
      - "docs/kingbrain/PLAN/PLAN.md"
      - "docs/kingbrain/PLAN/result-*.md"
      - ".collab/spec.lock.yaml"
      - "docs/kingbrain/POR.md"
    checks:
      - type: text_section
        file_glob: ["docs/kingbrain/PLAN/PLAN.md","docs/kingbrain/PLAN/result-*.md"]
        require_sections: ["What", "Why", "How", "Acceptance"]
      - type: cmd
        run: "kb tools check-por --por docs/kingbrain/POR.md --plan docs/kingbrain/PLAN/PLAN.md --spec ./.collab/spec.lock.yaml"
    reject_message: "AOC-1: PLAN 与 POR/Spec 不对齐或缺少 Acceptance。"

  - id: AOC-2
    name: Sources & Licenses
    must_pass: true
    evidence:
      - "docs/kingbrain/BORROW/README.md"
      - "docs/kingbrain/BORROW/Sources.md"
      - "docs/kingbrain/PLAN/manifest.json"
      - "docs/kingbrain/PLAN/manifest-*.json"
      - "data/sbom/**"
    checks:
      - type: manifest_sources
        file_glob: ["docs/kingbrain/PLAN/manifest*.json"]
        require_fields: ["repo","ref","license"]
      - type: cmd
        run: "kb tools license-scan --policy=configs/sbom-policy.yaml"
      - type: cmd
        run: "kb tools sbom-verify --format spdx-json@3.0 --path=data/sbom/"
    reject_message: "AOC-2: 来源/许可证不全或与 SBOM 不一致。"

  - id: AOC-3
    name: Causal mapping
    must_pass: true
    evidence:
      - "docs/kingbrain/PLAN/PLAN.md"
      - ".collab/audit/events-*.jsonl"
    checks:
      - type: text_section
        file_glob: ["docs/kingbrain/PLAN/PLAN.md","docs/kingbrain/PLAN/result-*.md"]
        require_sections: ["Causal Mapping"]
      - type: events_link
        file_glob: [".collab/audit/events-*.jsonl"]
        min_count: 1
    reject_message: "AOC-3: 缺少因果映射或事件证据。"

  - id: AOC-4
    name: Unified diff only & allowlist
    must_pass: true
    evidence:
      - "docs/kingbrain/DIFF/*.patch"
      - ".collab/paths.allowlist.yaml"
    checks:
      - type: diff_minimal
        patch_glob: ["docs/kingbrain/DIFF/*.patch"]
        forbid_full_file: true
      - type: allowlist_enforce
        allowlist_file: ".collab/paths.allowlist.yaml"
        patch_glob: ["docs/kingbrain/DIFF/*.patch"]
    reject_message: "AOC-4: 非最小 diff 或越权路径。"

  - id: AOC-5
    name: Evidence pack
    must_pass: true
    evidence:
      - "data/sbom/**"
      - "reports/**"
    checks:
      - type: cmd
        run: "kb tools sbom-verify --path=data/sbom/"
      - type: cmd
        run: "kb tools kyverno-verify ./k8s/manifests"
      - type: attestation_digest
        pattern: "^sha256:[0-9a-f]{64}$"
        files_glob: ["reports/**","docs/kingbrain/**","*.md","*.yaml"]
    reject_message: "AOC-5: 证据包不完整或摘要无效。"

  - id: AOC-6
    name: Rollback path
    must_pass: true
    evidence:
      - "k8s/**/patch-*.yaml"
      - "docs/kingbrain/PLAN/PLAN.md"
    checks:
      - type: image_pinned_digest
        file_glob: ["k8s/**/patch-*.yaml"]
        require_digest: true
      - type: text_section
        file_glob: ["docs/kingbrain/PLAN/PLAN.md","docs/kingbrain/PLAN/result-*.md"]
        require_sections: ["Rollback"]
    reject_message: "AOC-6: 未固定镜像 digest 或缺少回滚步骤。"

  - id: AOC-7
    name: Post-verification
    must_pass: true
    evidence:
      - "reports/kb-validate.xml"
    checks:
      - type: cmd
        run: "kb tools validate --config ./.collab/kb-validate/config.yaml"
      - type: junit_pass
        file: "reports/kb-validate.xml"
        require_zero_failures: true
    reject_message: "AOC-7: 后置验证不达标或报告缺失。"
