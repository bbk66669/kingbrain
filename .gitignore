# =========================
# .gitignore （可直接复制）
# =========================

# -------- Python / 通用 ----------
.venv/
**/.venv/
**/__pycache__/
*.py[cod]
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.ipynb_checkpoints/
.python-version

# -------- 构建 / 覆盖 / 分发 ----------
build/
dist/
*.egg-info/
*.egg
.eggs/
pip-wheel-metadata/
htmlcov/
.coverage
.coverage.*
coverage.xml

# -------- 编辑器 / 系统杂项 ----------
.vscode/
.idea/
.DS_Store
Thumbs.db

# -------- 日志 / 临时 ----------
*.log
*.tmp
*.swp
*.swo

# -------- 项目产物（数据库 / 日志 / 缓存） ----------
insight/*.db
insight/*.sqlite*
insight/embed_cache.*
insight/*.tar.gz
insight/all_labels.json
insight/chunks.json
insight/dead_code.txt
insight/full_files.json
insight/live_files.json
insight/min_lines_stats.json
insight/ingest_stats.json
insight/search_log.csv
insight/chunks_report.html
insight/.grimp_cache/
insight/DROP/

# -------- 反选需要保留的 JSON（若将来有上层规则忽略 *.json 时生效） ----------
!insight/grafana_dashboard.json
!insight/out.json

# -------- 环境变量 / 秘钥 ----------
# 任意位置的 env 文件
**/.env
**/.env.*
**/*.env
!**/.env.example
!insight/.env.example
# direnv
**/.envrc

# 常见证书 / 私钥（避免误提交）
*.pem
*.p12
*.pfx
*.der
*.crt
*.key

# -------- 下载的二进制 ----------
kyverno-cli_*.tar.gz

# -------- 只忽略这个“单个文件”，不影响同目录其他文件 ----------
insight/extsvc.json
.aider*

# runtime audit logs (do not commit)
/.collab/audit/
/.collab/**/events-*.jsonl