#!/usr/bin/env bash
# bootstrap 版：validate / lock-hash 的“占位通过”，不触碰你的原始规则
set -euo pipefail
case "${1:-}" in
  validate)   echo "OK: bootstrap validate passed (FAKE)";;
  lock-hash)  echo "OK: bootstrap lock-hash updated (FAKE)";;
  *) echo "usage: kb-tools {validate|lock-hash}"; exit 0;;
esac
